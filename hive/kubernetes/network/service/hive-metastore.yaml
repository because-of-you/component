apiVersion: v1
kind: Service
metadata:
  name: hive-metastore-service
  labels:
    app: hive-metastore-service
spec:
  #  ports:
  #    - port: 2181
  #      name: client
  #      targetPort: 2181
  #      protocol: TCP

  clusterIP: None
  selector:
    app: hive-metastore
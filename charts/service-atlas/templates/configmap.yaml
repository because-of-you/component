apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service-atlas.fullname" . }}-config
  labels:
    {{- include "service-atlas.labels" . | nindent 4 }}
data:
  catalogue.json: |-
    {{- .Values.catalogue | toPrettyJson | nindent 4 }}

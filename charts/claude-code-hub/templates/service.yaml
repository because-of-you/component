apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-code-hub.fullname" . }}
  labels:
    {{- include "claude-code-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "claude-code-hub.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-charts.fullname" . }}
  labels:
    {{- include "test-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "test-charts.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}

